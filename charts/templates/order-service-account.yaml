{{- if .Values.order.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.order.serviceAccount.name }}
  labels:
    app: {{ .Values.order.serviceAccount.name }}
  {{- with .Values.order.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.order.serviceAccount.automount }}
{{- end }}
