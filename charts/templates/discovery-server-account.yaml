{{- if .Values.discovery.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.discovery.serviceAccount.name }}
  labels:
    app: {{ .Values.discovery.serviceAccount.name }}
  {{- with .Values.discovery.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.discovery.serviceAccount.automount }}
{{- end }}
