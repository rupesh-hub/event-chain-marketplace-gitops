apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ecm

spec:
  selector:
    app: frontend-deployment
  ports:
    - port: 4200
      targetPort: 80
      protocol: TCP
  type: LoadBalancer